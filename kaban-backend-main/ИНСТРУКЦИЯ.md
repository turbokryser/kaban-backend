# 📖 Подробная инструкция по запуску проекта

## Что такое Docker?

**Docker** — это программа, которая позволяет запускать приложения в изолированных контейнерах. Представьте, что это как виртуальная машина, но легче и быстрее.

**Зачем нужен Docker?**
- Не нужно вручную устанавливать MySQL и настраивать базу данных
- Не нужно настраивать Python окружение
- Всё запускается одной командой
- Работает одинаково на Windows, Mac и Linux

**Но Docker НЕ обязателен!** Можно запустить проект и без него (см. раздел "Вариант 2" ниже).

---

## 🐳 Вариант 1: Запуск с Docker (Рекомендуется - проще всего)

### Шаг 1: Установка Docker

#### Windows:
1. Перейдите на сайт: https://www.docker.com/products/docker-desktop
2. Нажмите "Download for Windows"
3. Скачайте и установите Docker Desktop
4. После установки запустите Docker Desktop (иконка появится в трее)
5. Дождитесь, пока Docker полностью запустится (иконка перестанет мигать)

#### Mac:
1. Перейдите на: https://www.docker.com/products/docker-desktop
2. Выберите версию для Mac (Intel или Apple Silicon)
3. Скачайте и установите
4. Запустите Docker Desktop из Applications

#### Linux (Ubuntu/Debian):
```bash
# Установка Docker
sudo apt update
sudo apt install docker.io docker-compose
sudo systemctl start docker
sudo systemctl enable docker

# Добавьте себя в группу docker (чтобы не использовать sudo)
sudo usermod -aG docker $USER
# После этого нужно перелогиниться
```

### Шаг 2: Проверка установки Docker

Откройте командную строку (PowerShell на Windows, Terminal на Mac/Linux) и выполните:

```bash
docker --version
docker-compose --version
```

Если видите версии — всё установлено правильно!

### Шаг 3: Настройка проекта

1. **Откройте папку проекта** в проводнике/файловом менеджере

2. **Создайте файл `.env`**:
   - Скопируйте файл `env.example` и переименуйте в `.env`
   - Или создайте новый файл `.env` в корне проекта

3. **Откройте `.env` файл** в любом текстовом редакторе и измените:

```env
# База данных (для Docker не трогайте, уже настроено)
DATABASE_URL=mysql+pymysql://kaban_user:kaban_password@db:3306/kaban_db

# ВАЖНО! Измените этот ключ на любой случайный набор символов
SECRET_KEY=мой-секретный-ключ-12345-измените-это

# Остальное можно оставить как есть
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7
HOST=0.0.0.0
PORT=8000
```

**Важно:** `SECRET_KEY` должен быть уникальным! Используйте любой набор символов, например: `abc123xyz789secretkey2024`

### Шаг 4: Запуск проекта

Откройте командную строку/терминал в папке проекта и выполните:

```bash
docker-compose up --build
```

**Что происходит:**
- Docker скачает образы MySQL и Python (первый раз может занять 5-10 минут)
- Создаст базу данных MySQL
- Импортирует схему базы данных
- Запустит API сервер

**Вы увидите логи** в консоли. Когда увидите что-то вроде:
```
INFO:     Uvicorn running on http://0.0.0.0:8000
```

Значит всё запущено! 🎉

### Шаг 5: Проверка работы

Откройте браузер и перейдите на:
- **API документация:** http://localhost:8000/docs
- **Проверка здоровья:** http://localhost:8000/health

Если видите страницу с документацией API — всё работает!

### Остановка проекта

Нажмите `Ctrl+C` в терминале, где запущен Docker, или выполните:
```bash
docker-compose down
```

### Полезные команды Docker

```bash
# Запустить в фоновом режиме
docker-compose up -d

# Остановить
docker-compose down

# Посмотреть логи
docker-compose logs

# Пересобрать всё заново
docker-compose up --build --force-recreate
```

---

## 🖥️ Вариант 2: Запуск без Docker (Ручная установка)

Если не хотите устанавливать Docker, можно запустить всё вручную.

### Шаг 1: Установка Python

1. Скачайте Python 3.11+ с https://www.python.org/downloads/
2. При установке **обязательно отметьте** "Add Python to PATH"
3. Проверьте установку:
```bash
python --version
# Должно показать Python 3.11.x или выше
```

### Шаг 2: Установка MySQL

#### Windows:
1. Скачайте MySQL Installer: https://dev.mysql.com/downloads/installer/
2. Выберите "MySQL Installer for Windows"
3. При установке запомните пароль root пользователя!
4. Установите MySQL Server

#### Mac:
```bash
# Используя Homebrew
brew install mysql
brew services start mysql
```

#### Linux (Ubuntu/Debian):
```bash
sudo apt update
sudo apt install mysql-server
sudo systemctl start mysql
sudo systemctl enable mysql
```

### Шаг 3: Создание базы данных

Откройте MySQL командную строку или MySQL Workbench и выполните:

```sql
CREATE DATABASE kaban_db;
CREATE USER 'kaban_user'@'localhost' IDENTIFIED BY 'kaban_password';
GRANT ALL PRIVILEGES ON kaban_db.* TO 'kaban_user'@'localhost';
FLUSH PRIVILEGES;
```

### Шаг 4: Импорт схемы базы данных

В командной строке (в папке проекта) выполните:

**Windows:**
```bash
mysql -u kaban_user -p kaban_db < "drawSQL-mysql-export-2025-12-09 (1).sql"
```

**Mac/Linux:**
```bash
mysql -u kaban_user -p kaban_db < drawSQL-mysql-export-2025-12-09\ \(1\).sql
```

Введите пароль: `kaban_password`

### Шаг 5: Создание виртуального окружения Python

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**Mac/Linux:**
```bash
python3 -m venv venv
source venv/bin/activate
```

После активации в начале строки терминала появится `(venv)`

### Шаг 6: Установка зависимостей

```bash
pip install -r requirements.txt
```

Это может занять несколько минут.

### Шаг 7: Настройка переменных окружения

1. Создайте файл `.env` в корне проекта
2. Скопируйте содержимое из `env.example`
3. Измените `DATABASE_URL`:

```env
DATABASE_URL=mysql+pymysql://kaban_user:kaban_password@localhost:3306/kaban_db
SECRET_KEY=мой-секретный-ключ-измените-это
```

### Шаг 8: Запуск сервера

```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

Вы увидите:
```
INFO:     Uvicorn running on http://0.0.0.0:8000
```

Откройте http://localhost:8000/docs в браузере!

---

## 🧪 Тестирование API

### 1. Регистрация пользователя

Откройте http://localhost:8000/docs и найдите `POST /auth/register`

Нажмите "Try it out" и введите:
```json
{
  "username": "testuser",
  "email": "test@example.com",
  "password": "password123"
}
```

Нажмите "Execute". Вы получите `access_token` и `refresh_token` — сохраните их!

### 2. Создание команды

Найдите `POST /teams`, нажмите "Authorize" (замочек), вставьте ваш `access_token`, затем:

```json
{
  "name": "Моя команда"
}
```

### 3. Создание проекта

`POST /projects`:
```json
{
  "name": "Мой проект",
  "description": "Описание проекта",
  "team_id": 1
}
```

### 4. Просмотр доски

`GET /projects/1/board` — увидите доску с колонками и задачами!

---

## ❓ Решение проблем

### Проблема: "Docker не запускается"
- Убедитесь, что Docker Desktop запущен
- На Windows может потребоваться включить WSL2
- Перезагрузите компьютер после установки Docker

### Проблема: "Порт 8000 занят"
- Измените порт в `.env`: `PORT=8001`
- Или остановите программу, использующую порт 8000

### Проблема: "Не могу подключиться к базе данных"
- Проверьте, что MySQL запущен
- Проверьте логин и пароль в `.env`
- Убедитесь, что база данных создана

### Проблема: "ModuleNotFoundError"
- Убедитесь, что виртуальное окружение активировано
- Выполните: `pip install -r requirements.txt`

### Проблема: "Permission denied" (Linux/Mac)
- Используйте `sudo` для команд Docker (если не добавили пользователя в группу)
- Или добавьте пользователя: `sudo usermod -aG docker $USER`

---

## 📞 Нужна помощь?

1. Проверьте логи: `docker-compose logs` (если используете Docker)
2. Убедитесь, что все шаги выполнены правильно
3. Проверьте версии: Python 3.11+, MySQL 8.0+, Docker последней версии

---

## 🎯 Краткая памятка

**С Docker (проще):**
1. Установить Docker Desktop
2. Создать `.env` файл
3. `docker-compose up --build`
4. Открыть http://localhost:8000/docs

**Без Docker:**
1. Установить Python и MySQL
2. Создать базу данных
3. Установить зависимости: `pip install -r requirements.txt`
4. Создать `.env` файл
5. Запустить: `uvicorn main:app --reload`

Удачи! 🚀




